02/09/2026 LipidXplorer 1.5.0 - batch mode logging

- Fixed: batch mode appeared to do nothing on macOS and Linux and was
  reported as working only on Windows. It was in fact running correctly on
  all three platforms; none of its progress was visible. Worker processes
  are started with the 'spawn' method, so they do not inherit the logger the
  GUI installs over print(), and in a packaged build their output goes to a
  null sink. The debug window therefore stayed empty between "Using N worker
  process(es)" and the first finished sample -- more than a minute per
  sample, which reads as a hang. Workers now write their progress and their
  tracebacks to the shared batch log.

- Every line in the batch log now names the process that wrote it: "[MAIN]"
  for the controller, "[W3 <sample>]" for a worker together with the sample
  it is working on. The label is applied where the log is written rather
  than at each call site, so output from the import and MFQL code -- none of
  which knows a batch is running -- is attributable too. A worker keeps its
  number as the pool moves it on to the next sample.

- Fixed: a sample whose worker crashed and a sample that simply had no hits
  were reported by the same line, and the error captured from the worker was
  never printed. The two are now distinct, failures carry the traceback, and
  progress is reported as (n/total).

- Fixed: the debug window was written to directly from the batch thread.
  wxWidgets allows GUI access only from the main thread; wxMSW tolerates the
  violation, wxOSX and wxGTK do not. The window is now filled by a timer on
  the main thread that tails the log file.

- Fixed: warnings raised while merging per-sample results were discarded,
  because only print() was redirected to the log and warnings are written to
  stderr.

- Fixed: delimiter detection for per-sample result files failed for every
  file. Detection reads the first 4096 bytes, which stops mid-line on rows
  as wide as these, and the truncated last line made csv.Sniffer reject the
  sample. It fell back to comma silently. That happened to be correct, since
  the files are written comma-separated, but a tab-limited run would have
  collapsed every merged row into a single column.

- Fixed: print() stayed redirected into a finished run's log file for the
  rest of the session, so later output was filed away instead of shown.

- Fixed: with "Save per sample result" unticked, the log listed per-sample
  CSV paths without recording that they were then deleted, and an empty
  per_sample_results directory was left behind when no sample produced hits.

28/08/2026 LipidXplorer 1.5.0 - build system

- Dependency management moved from Conda to uv: pyproject.toml plus a
  committed uv.lock, with Python 3.12 pinned in .python-version.
  environment_ubuntu.yml and environment_windows.yml are kept as a supported
  alternative for conda users, but uv.lock is authoritative.

- Builds for Windows, Linux, and macOS (Apple Silicon) are produced by GitHub
  Actions from a single PyInstaller spec file. macOS now gets a proper .app
  bundle. There is no Intel macOS build: numba and llvmlite no longer publish
  macOS x86_64 wheels, so that platform cannot be built without compiling LLVM
  from source.

- Fixed: on Linux the application crashed at startup on machines without the
  X11 development package installed.

- Fixed: on Linux and macOS the optional groups.txt occupation-threshold file
  was never found, because its path was built with a hardcoded backslash. The
  setting silently had no effect on those platforms.

- Fixed: `lx.fileReader.mzAPI.mzWiff` was imported through a doubled module
  path (`lx.fileReader.lx.fileReader.mzAPI.mzWiff`) left behind by the
  Python 3 migration; that path is now corrected. Opening a SCIEX .wiff
  file still fails, however: `mzWiff` itself imports `wiffbridge`, a module
  that is not part of this repository, so .wiff reading remains
  unavailable.

- Removed roughly 17 MB of unused libraries from the distributed bundle
  (the tkinter stack, Pythonwin, setuptools).

- Ported lpdxLXMerge.py to Python 3. Removed lx/mfql/syntaxParser.py, which
  was unreachable and could not be parsed by Python 3.

06/05/2026 LipidXplorer 1.5

- Code base migrated from python 2.7 to 3.12

- New feature: Batch mode: Use multiprocessing to process samples separately and finally proved unified result

Must have columns for batch mode:
LipidSpecies
LipidClass
ScanPolarity

Good to have for proper output format:
Mass
Intensity
PrecursorIntensity
FragmentXIntensity (X:A,B,C..)
/

- Isotopic correction — two independent mechanisms

LipidXplorer applies two separate corrections when isotopic correction is
enabled, both controlled by the same setting, but solving different
problems. **They are now both visible in the dump for the first time.**

### Type II correction — cross-molecule isotope contamination

**Problem it solves:** two *different* molecules can happen to land close
enough in mass that one's natural isotope peak (its own ¹³C-containing
population) overlaps with the other's monoisotopic peak. When this
happens, some of what's measured "as" the second molecule is really
contamination from the first.

**How it works:** searches for a neighboring entry at the theoretically
expected isotope spacing for the relevant charge state (`1.0033/charge`,
`2 × 1.0033/charge`, etc., within a tolerance window). If found, estimates
and subtracts the contaminating contribution.

**Where it's checked:** at the precursor level (MS1) and at the fragment
level, both between different precursors' fragments (interscan) and
within a single precursor's own MS2 spectrum (intrascan).

**Marked with:** `*` in the dump's isotope-flag column.

### Type I correction — a molecule's own isotope envelope

**Problem it solves:** even a perfectly pure, isolated compound isn't
measured as one single peak. Natural isotope abundance means only a
fraction of its molecular population appears at the monoisotopic (M0)
mass — the rest genuinely exist at M+1, M+2, etc. Measuring only M0
under-represents the compound's true total abundance, more so for larger
molecules (more carbons → more of the population statistically carries at
least one heavy isotope).

**How it works:** divides the measured M0 intensity by the theoretical
fraction of the total isotope envelope that M0 represents (computed from
the molecule's own elemental formula). This scales the reported intensity
up to estimate the true total abundance.

**Applies to:** essentially every matched entry, regardless of whether a
Type II neighbor was ever found — it depends only on the molecule's own
formula, not on any other nearby peak.

**Marked with:** `+` in the dump's isotope-flag column (newly added — this
correction previously ran silently with no visual indication it had
touched an entry).

### Combined flag

| Flag | Meaning |
|---|---|
| `' '` (blank) | Neither correction applied |
| `*` | Type II only |
| `+` | Type I only |
| `#` | Both applied to the same entry |

### Correction provenance (new)

Each corrected entry now carries a description of what caused the
correction, appended after the mark list in the dump:
```
M+1 from 715.5112 (CardiolipinIsotope) [Observed delta: 0.50265; Expected delta(charge-2): 0.50165]
```
This shows the isotope level, the contributing entry's mass and owning
query, the observed mass gap to that contributor, and the theoretically
expected gap at the charge state used to accept the match. Type I
correction similarly logs the `monoisotopicRatio` used.

---

## 3. What was fixed in this release

- **Order-dependent marking and correction** — the same query set could
  previously produce different marks and different corrected intensities
  depending on execution order. Fixed; results are now deterministic
  regardless of query order.
- **Isotope correction was not charge-aware at the MS2 level.** Precursor
  (MS1) correction always correctly accounted for charge state; MS2
  correction was hardcoded to assume charge 1, meaning doubly- (or more)
  charged species were checked at the wrong mass spacing and could miss
  real isotope relationships. Fixed for both the cross-precursor and
  within-spectrum MS2 correction paths.
- **The isotope flag was not reliably being set**, meaning corrected
  intensities could appear in the dump with no indication a correction had
  been applied. Fixed for both correction types.
- **Type I correction had no visibility at all** — it silently applied to
  nearly every matched entry with zero marking, which could make dump
  intensities look inconsistent with the output report for reasons that
  weren't traceable. Fixed (see flag table above).



09/10/2024 LipidXplorer 1.2.9

Bug fixes
- fixing removePermutations to leave the precursor with the lowest absolute errppm instead of the one with the lowest
  errppm value.
- renewing help links

26.11.2019 LipidXplorer 1.2.8.1

Bug Fixes
- Fixed broken Import Settings "Select a Configuration Save As..." and "Delete"
- Added additional log output to print time limits applied to scan selection in seconds and minutes

21.09.2019 LipidXplorer 1.2.8

New Functionality
- Added MSFilter and MSMSFilter
The MSFilter and MSMSFilter takes a value between  0 and 1 can be chosen representing the fraction of scans in which a certain peak has to be present.
The filter is applied for both MS and MS/MS according to the given import setting for each scan type. 
For each precursor ion all associated MS/MS scans are collected according to selection window. Counting of MS/MS scans and association of precursor is done automatically. 
Accordingly, the number of MS/MS scan can vary for each precursor ion in the shotgun acquisition, as might occur when using a DDA strategy.

- Improved mzML file loading speed from O(n^2) to O(n).
This improves loading speed of mzML files significantly and brings them on par with mzXML files considering import of data.

- Added application and taskbar icons.

- (Developers) Anaconda package management.
LipidXplorer now comes with a dependency definition for an Anaconda environment for development under Windows. Please check the GitLab README.md file for further information.

- LipidXplorer executable distributions (convenience binaries).
LipidXplorer is now distributed as a Windows and Linux archive containing executables that come with all dependencies included. These can be downloaded from https://lifs.isas.de/lipidxplorer.

Bug Fixes
- Updated Wiki locations to https://lifs.isas.de/wiki
- Fixed MFQL editor dialog closing error on Windows 10 and Python 2.7.
- Fixed application not exiting properly due to non-terminated threads.
- Improved file handle closing by switching to auto closing behaviour with 'with'.
- Fixed output file creation for user accounts with '.' characters in path.
- Fixed UI label truncations and spacing issues on Windows 10.
- Updated wxPython dependency from 3.0 to 4.0.4.

Removed Functionality*
- Removed sample grouping
- Removed DTA/CSV + PIS support for input spectra

Deprecated Functionality
- The mzXML format is deprecated and will be removed for the LipidXplorer 1.3.0 release.

* If you rely on this functionality, please contact us at lifs-support@isas.de

12.08.2008

- new function isStandard(<variable>, "<sample>", "<scope>"). Place it in SUCHTHAT section. 
  A scan is started to find <variable> in <sample>. Once found it is declared as the standard and the
  intensities of all other peaks in <scope> are recalculated as ratio. 
  Example: isStandard(varStandard, "hilde01", "MS1+")

- new function for addressing groups of samples with the help of placeholders:

    Patterns are Unix shell style:
    
    *       matches everything
    ?       matches any single character
    [seq]   matches any character in seq
    [!seq]  matches any char not in seq

  Example: FA1.intensity["*hilde0[1-9]*"] > FA1.intensity["*hilde1[0-9]*"]

- also possible: column(FA1, "*hilde0[1-9]*")

13.08.2008

- semi-new function: isStandard(<variable>, "<scope>"). With this syntax the standard is
  calculated for every single sample.

14.08.2008

- every peak (either MS or MS/MS) has now additionally the following information:
    . peak mean
    . peak median
    . peak variance
    . peak standard deviation
  all this info will be put out in the dump

22.08.2008

- isStandard() function works now for MS1+/- (! no MS2+/- !). The function should be placed in the SUCHTHAT
  section and has as attributes 1) a variable containing the marked standard and 2) the scope of
  the standard (MS1+, MS1-, MS2+ or MS2-). Next an example of an MFQL script identifying
  PC with calculating the standard:

	QUERYNAME = Phosphatidylcholine;
	DEFINE PR = 'C[36..50] H[30..200] N[1] O[8] P[1]' WITH DBR = (1.5,7.5), CHG = 1;
	DEFINE DietherPC = 'C44 H93 O6 N1 P1' WITH CHG = 1;
	DEFINE DietherPE = 'C45 H95 O6 N1 P1' WITH CHG = 1;

	IDENTIFY Phosphatidylcholine WHERE
		
		# marking
		PR IN MS1+ WITH TOLERANCE = 4 ppm OR
		DietherPC IN MS1+
	  
	SUCHTHAT
		isEven(PR.chemsc[C]) AND
		isStandard(DietherPC, "MS1+")
	  
	REPORT 
		MASS = "%4.4f" % "(PR.mass)";
		NAME = "PC [%d:%d]" % "((PR.chemsc)[C] - 8, (PR.chemsc)[db] - 1.5)";
		PRECURINTENS = PR.intensity;;

2.09.2008 

- de-isotoping was implemented for MS mode. The algorithm uses the sum compositions
  which where calculated with the used MFQL scripts. So, only molecular species of interest
  are considered for the de-isotoping. The algorithm is the following:
  1) sort MS spectrum increasingly.
  2) for every mass m which has a chemical sum composition assigned:
    3) look, if there is are masses i1, i2, i3 or i4 which could be isotopes, i.e.
      is there a mass i1 = m + 1.0033,
      is there a mass i2 = m + 2 * (1.0033), and so on ...
    4) calcuate the isotopic distribution of m for only 13C. Isotopes of other elements
       are so little for lipids, that we leave them beside. The distribution is binomial
       with a probability that a 13C occurs of 0.01082 <insert citation here>.
    5) substract calculated isotopic percentage from i1-4

- de-isotoping of MS/MS was revisited. The algorithm is the following:
  1) generate artificial PIS spectra P1, ..., Pn for fragments f1, ..., fn by collecting 
     precursor masses which have f1 (f2, ..., fn) in thier MS/MS spectrum.
  2) for every PIS spectrum Pi:
    3) for every mass m in Pi:
      4) look, if there is are masses i1, i2, i3 or i4 which could be isotopes
      5) calculate the isotopica distribution of the neutral loss of m accoding to
        <insert citation here>
      6) subtract the calculated isotopic percentage from i1-4 

1.10.2008

- implemented a debugging window in the GUI.

- GUI looks a bit more compact now

- update of the merging algorithm (for *.mzXML import). Average masses are now calculated 
  by intensity weighted average. This yields more accurate spectra.

9.10.2008

- switched off the cleaning procedure for *.mzXML imported files

- if more than one sum composition is found for a precursor mass, it will be reported
  ordered by identification error.

- Bug with *.mzXML file, where only MS/MS spectra are given is fixed.

23.10.2008

- new Function: complementMasterScan. Switch it on with a checkbox on the
  Run-Panel. It will produce the "complement MasterScan" of the current
  query, i.e. a MasterScan with all peaks which where not identified in
  the current run. It will be saved as <original MasterScan name>-complement.sc.

- the purpose is do blind queries for unknown or not excpected sum compositions.

18.11.2008

- new Function: DEFINE allows now to define a list of same variables with
  different names in one line. The user just writes a list:
    old:
      DEFINE FA1 = 'C[14..22] H[20..60] O[2] N[1]' WITH DBR = (0.0,6.0), CHG = 0;
      DEFINE FA2 = 'C[14..22] H[20..60] O[2] N[1]' WITH DBR = (0.0,6.0), CHG = 0;
      DEFINE FA3 = 'C[14..22] H[20..60] O[2] N[1]' WITH DBR = (0.0,6.0), CHG = 0;
    new:
      DEFINE (FA1, FA2, FA3) = 'C[14..22] H[20..60] O[2] N[1]' WITH DBR = (0.0,6.0), CHG = 0;
    
- new Function: DEFINE allow the definition of a list of fragments. For example:
    DEFINE FA = ('C14 H27 O2 N1',
                 'C14 H29 O2 N1',
                 'C14 H31 O2 N1',
                 'C15 H21 O2 N1',
                 'C15 H31 O2 N1');

2009-03-06

- adding a new Variable to TypeMFQL: currVars, which carries the current variables
  and is accessible for all functions
- adding a new function: avg(), which calculates the average of the attribute (which
  should be an intensity vector by now)

2009-07-02

- new function in the import which allows grouping the samples by letting the user put
  them into group folders. This does only work for *.mzXML until now.

2009-12-01

* fixed massrange error when using masses in DEFINE
* fixed problem with order of the *.mfql scripts in the Run Panel

2009-12-03

* implemented multiple charge recognition !NOTE!: it is to be tested!
  Lipids with charge unequal to 1, 0 or -1 are not able to account to
  isotopic correction.
* upgrade of the Exception handling.


